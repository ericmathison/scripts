#!/usr/bin/env ruby

require 'nokogiri'
require 'uri'

doc = Nokogiri::HTML($stdin.read)
doc.xpath("//a").each do |link|
  if !link['href'].nil?
    puts  URI.decode(link['href']) if link['href'][/\Ahttp/]
  end
end
